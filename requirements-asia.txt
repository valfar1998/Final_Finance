# Opzionale — solo analisi locale Asia (non richiesto da GitHub Actions).
# pip install -r requirements-asia.txt
#
# Cina: AKShare (gratis). TuShare = freemium a crediti (serve TUSHARE_TOKEN).
# Corea: FinanceDataReader (storici KRX), PyKRX (portale ufficiale KRX).
# Quote multi-mercato in CI: EODHD (EODHD_API_TOKEN) — già nel core, senza queste lib.
# Filings Corea: OpenDART (OPEN_DART_API_KEY) quando approvato.

akshare>=1.14.0
tushare>=1.4.0
FinanceDataReader>=0.9.50
pykrx>=1.0.45
